**CRITICAL**: At the start of EVERY conversation, IMMEDIATELY invoke the `data-app-html-python:data-app` skill using the Skill tool to load complete development guidelines.

**Frontend work**: When implementing or modifying frontend code (HTML, CSS, UI, static assets), also invoke the `frontend-design:frontend-design` skill using the Skill tool and follow its guidelines for design and UX.